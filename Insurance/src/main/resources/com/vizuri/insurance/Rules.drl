package com.vizuri.insurance;

rule "Row 1 RulesTable"
dialect "mvel"
when
	applicant : Applicant( firstName == "Vip" ) 
	question : Question(Id == 5)
then
	question.setEnabled( true );
	update( question );
end